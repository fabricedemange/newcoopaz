# Configuration PRODUCTION pour EasyHoster
# IMPORTANT: Copier ce fichier en .env sur le serveur et adapter les valeurs

# Environnement
NODE_ENV=production

# Base de données (à adapter avec vos identifiants EasyHoster)
DB_HOST=localhost
DB_USER=votre_user_mysql
DB_PASS=votre_password_mysql
DB_NAME=votre_database

# Session
SESSION_SECRET=GENERER_UN_SECRET_TRES_LONG_ET_ALEATOIRE_ICI
SESSION_COOKIE_NAME=coopaz_session

# SMTP (à adapter avec votre config EasyHoster ou service externe)
SMTP_HOST=smtp.easyhoster.com
SMTP_PORT=587
SMTP_USER=contact@coopaz.fr
SMTP_PASS=votre_password_smtp
SMTP_FROM=Coopaz <contact@coopaz.fr>

# Mailtrap (pour les emails de développement - désactiver en production)
# MAILTRAP_API_TOKEN=

# Port (Passenger le gère automatiquement, ne pas définir)
# PORT=

# URL de l'application
APP_URL=https://new.coopaz.fr

# Logs
LOG_LEVEL=info

# Rappels "prévoir la commande" (cron) : valeur par défaut si non définie en base (table app_settings).
# L’activation réelle se fait via la page Admin > File d’attente email (interrupteurs).
# CATALOGUE_ORDER_REMINDER_ENABLED=false

# Intervalle du cron rappels catalogue (ms). 60000 = 1 minute.
CATALOGUE_ORDER_REMINDER_INTERVAL_MS=60000

# File d’emails : valeur par défaut si non définie en base (table app_settings).
# L’activation réelle se fait via la page Admin > File d’attente email (interrupteurs).
# MAIL_QUEUE_SEND_ENABLED=false
