<%- include('partials/header', { title: pageTitle || 'Produits jamais vendus', user, role }) %>

<div class="admin-content-wrapper">
  <div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
      <h1 class="h4 mb-0" style="color: #7a5e12;"><i class="bi bi-box-seam me-2"></i><%= pageTitle %></h1>
      <a href="/caisse/stock" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left me-1"></i>Accueil Stock</a>
    </div>
    <div class="card border" style="border-color: rgba(212,166,23,0.25) !important;">
      <div class="card-body">
        <p class="text-muted mb-0">
          Liste des produits en catalogue qui n'ont jamais été vendus. Cette vue sera alimentée automatiquement lorsqu'il y aura du volume d'activité (historique de ventes).
        </p>
        <p class="mt-3 mb-0 small text-muted">
          Aucune donnée à afficher pour l'instant.
        </p>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
